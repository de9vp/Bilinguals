<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css"> 
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            @Html.ActionLink("MY BILINGUALS", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">

                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">@Html.ActionLink("Trang học của tôi", "Index", "Learn", null, new { @class = "nav-link" })</li>
                    <li class="nav-item">@Html.ActionLink("Đoạn hội thoại", "Index", "Dialogs", null, new { @class = "nav-link" })</li>
                    <li class="nav-item">@Html.ActionLink("Giới thiệu", "About", "Home", null, new { @class = "nav-link" })</li>
                    <li class="nav-item">
                        @if (User.IsInRole("Administrator"))
                        {
                            @Html.ActionLink("Admin", "", "", null, new { @class = "nav-link" })
                        }
                    </li>
                </ul>

                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </nav>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Duy Phuc To</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")

    @*<script src="~/Scripts/jquery.blockUI.js"></script>

    <script type="text/javascript">

        $(function () {
            var defaults = $.blockUI.defaults;
            $.blockUI.defaults = {
                ...defaults,
                message: 'Please wait...',
                baseZ: 1055,
                overlayCss: {
                    ...defaults.overlayCSS,
                    opacity: '.3'
                },
                css: {
                    ...defaults.css,
                    border: 'none',
                    padding: '15px',
                    backgroundColor: '#000',
                    '-webkit-border-radius': '6px',
                    '-moz-border-radius': '6px',
                    opacity: .4,
                    color: '#fff'
                },
            };

            $(document).ajaxStart($.blockUI).ajaxStop($.unblockUI);
        });
    </script>*@

    @RenderSection("scripts", required: false)
</body>
</html>
